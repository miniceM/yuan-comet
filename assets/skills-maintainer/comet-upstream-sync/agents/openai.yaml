interface:
  display_name: "Comet 上游同步"
  short_description: "镜像上游 master 并集成到企业主线"
  default_prompt: "使用 $comet-upstream-sync 同步上游 rpamis/comet 最新代码到 master 并安全集成到 enterprise/main。"
policy:
  allow_implicit_invocation: false
